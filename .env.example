# =============================================================================
# DAAssist - Environment Configuration Template
# =============================================================================
# Copia questo file in .env e modifica i valori secondo le tue esigenze
# cp .env.example .env

# =============================================================================
# Database PostgreSQL
# =============================================================================
POSTGRES_USER=daassist_user
POSTGRES_PASSWORD=change_this_secure_password_in_production
POSTGRES_DB=daassist_db
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Database URL completo (usato dal backend)
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# =============================================================================
# Backend FastAPI
# =============================================================================
# Secret key per JWT - CAMBIARE IN PRODUZIONE!
# Genera con: openssl rand -hex 32
SECRET_KEY=change_this_secret_key_to_a_random_string_in_production_use_openssl_rand_hex_32

# Algoritmo JWT
ALGORITHM=HS256

# Scadenza token (minuti)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS Origins (separati da virgola)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Ambiente (development | production)
ENVIRONMENT=development

# Log level (DEBUG | INFO | WARNING | ERROR)
LOG_LEVEL=INFO

# =============================================================================
# Frontend React/Vite
# =============================================================================
# URL base API backend
VITE_API_URL=http://localhost:8000/api/v1

# Nome applicazione
VITE_APP_NAME=DAAssist

# Ambiente frontend
VITE_ENVIRONMENT=development

# =============================================================================
# Docker Compose
# =============================================================================
# Porte esposte
BACKEND_PORT=8000
FRONTEND_PORT=5173
DB_PORT=5432

# Volumi persistenti
POSTGRES_DATA=./pgdata

# =============================================================================
# Deployment LXC/Proxmox (opzionale)
# =============================================================================
# Dominio pubblico (per SSL)
# DOMAIN=daassist.example.com

# Email per Let's Encrypt
# LETSENCRYPT_EMAIL=admin@example.com

# =============================================================================
# Backup (opzionale)
# =============================================================================
# Directory backup
# BACKUP_DIR=./backups

# Ritenzione backup (giorni)
# BACKUP_RETENTION_DAYS=30

# =============================================================================
# Email SMTP (opzionale - feature futura)
# =============================================================================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@daassist.local

# =============================================================================
# Redis (opzionale - feature futura per cache)
# =============================================================================
# REDIS_HOST=redis
# REDIS_PORT=6379
# REDIS_PASSWORD=
# REDIS_DB=0

# =============================================================================
# Gestionale ERP Integration (opzionale - feature futura)
# =============================================================================
# GESTIONALE_DB_HOST=sqlserver.local
# GESTIONALE_DB_PORT=1433
# GESTIONALE_DB_USER=daassist_readonly
# GESTIONALE_DB_PASSWORD=
# GESTIONALE_DB_NAME=GestionaleDB
